<?php
/** Generated by bin/local-rig.sh. Not a real site; do not copy these values. */
define( 'DB_NAME', 'citecue_rig' );
define( 'DB_USER', '__DB_USER__' );
define( 'DB_PASSWORD', '' );
define( 'DB_HOST', '__DB_HOST__' );
define( 'DB_CHARSET', 'utf8mb4' );
define( 'DB_COLLATE', '' );

$table_prefix = 'wp_';

define( 'WP_DEBUG', true );
define( 'WP_DEBUG_LOG', true );
define( 'WP_DEBUG_DISPLAY', false );

// The rig runs the plugin's jobs itself, so a page view never triggers one and
// the cold/warm distinction stays under the test's control.
define( 'DISABLE_WP_CRON', true );

// Point the plugin at the stub. This is the plugin's own supported override for
// a self-hosted or staging origin — see Citecue_Settings::pinned_api_base().
define( 'CITECUE_API_BASE', 'http://127.0.0.1:__API_PORT__' );

define( 'AUTH_KEY', 'rig' ); define( 'SECURE_AUTH_KEY', 'rig' ); define( 'LOGGED_IN_KEY', 'rig' );
define( 'NONCE_KEY', 'rig' ); define( 'AUTH_SALT', 'rig' ); define( 'SECURE_AUTH_SALT', 'rig' );
define( 'LOGGED_IN_SALT', 'rig' ); define( 'NONCE_SALT', 'rig' );

define( 'WP_HOME', 'http://127.0.0.1:__WP_PORT__' );
define( 'WP_SITEURL', 'http://127.0.0.1:__WP_PORT__' );

if ( ! defined( 'ABSPATH' ) ) {
	define( 'ABSPATH', __DIR__ . '/' );
}
require_once ABSPATH . 'wp-settings.php';
